# Windows Distributable

Run `ASCIIBirthdayGenerator.exe` to create personalized birthday executables.

The app is standalone and does not require Python. `SHA256SUMS.txt` contains the SHA-256 checksum for the executable in this folder.
